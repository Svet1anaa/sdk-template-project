# Module 1 — Reflection

## What I did - 

I forked the sdk-template-project repository to my GitHub account and cloned it to my machine. Also I read through the README.md and ARCHITECTURE.md files to understand the system structure, and found a difference between the documentation and the actual state of the code. 

## What I did not understand at first-

I did not understand at first the difference between ‘docker compose down’ and ‘docker compose down -v’, and why forking on GitHub is a separate step from cloning - I did not realize at first that ‘git push’ only works against your own fork, not the original repository.

## What I would do differently - 

I would check available disk space before starting because most of the time I spent today was a troubleshooting disk and Docker configuration issues rather than on exploring the actual codebase. I would also set Docker's storage location to a non-system drive from the very first installation.

## How long this took me -

About 3 hours, most of it I spent on resolving environment and disk space issues rather than working with the application itself.



